# This static Vite site currently requires no client-side environment variables.
# Keep service credentials and database passwords in server-side hosting settings only.
# Netlify Forms must be enabled on the deployment for contact/availability submissions.
