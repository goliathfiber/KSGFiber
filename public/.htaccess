<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /kramer/

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule . /kramer/index.html [L]
</IfModule>
